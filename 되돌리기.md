# 되돌리기 (롤백) 안내

보안 작업(2026-07-27)에서 문제가 생겼을 때 원래대로 돌리는 방법입니다.
**웹 되돌리기와 시트 되돌리기는 서로 상관없이 따로따로 할 수 있습니다.**

---

## 1. 웹 화면을 원래대로

명령 프롬프트에서 이 폴더로 간 뒤 아래 두 줄을 그대로 실행하세요.

```bash
git revert --no-edit before-security-20260727..HEAD
```

```bash
git push origin main
```

1~2분 뒤 사이트가 예전 모습으로 돌아옵니다.
되돌린 뒤에도 작업 내용은 사라지지 않고 기록에 남아 있어, 나중에 다시 살릴 수 있습니다.

**되돌아가는 지점**: `before-security-20260727` (= `ea761b4`, 보안 작업 직전)

---

## 2. 시트를 원래대로

### 시트 공유를 잠갔는데 자료가 안 보일 때

시트 → **공유** → **「링크가 있는 모든 사용자」** 로 다시 바꾸세요.
바꾸는 즉시 복구됩니다. 웹은 건드릴 필요 없습니다.

### 스크립트를 새로 배포했는데 저장이 안 될 때

Apps Script → **배포 › 배포 관리 › ✏️ › 버전** 에서 **예전 버전을 고르고 배포**하세요.
직전 버전은 `2026-07-27-consult2` 입니다.

### 저장 주소(/exec)를 새로 만들었는데 문제가 생길 때

**배포 관리**에서 옛 배포를 다시 살리고(보관처리 해제), 새 배포를 보관처리하세요.
웹의 기본 주소는 `js/sheet-api.js` 의 `DEFAULT_WRITE_URL` 한 줄입니다.

---

## 3. 단계별로 「여기서 틀어지면」

| 단계 | 증상 | 대처 |
|---|---|---|
| 스크립트 새 버전 배포 | 저장이 안 됨 | 위 2번 — 예전 버전으로 재배포 |
| 시트 공유 「제한됨」 | 화면에 자료가 안 뜸 | 위 2번 — 공유를 원래대로 |
| 웹 새로 올림 | 화면이 이상함 | 위 1번 — 두 줄 실행 |
| 저장 주소 교체 | 특정 사람만 저장 실패 | 그 사람 브라우저에서 ⚙ → **기본 주소 사용** → 저장 |

---

## 3-2. 암호 잠금을 없애고 예전처럼 되돌리기 (2026-07-28)

암호 때문에 아무도 못 들어가는 상황이면 아래 둘 중 하나만 하면 됩니다.

### 빠른 방법 — 시트 스크립트만 되돌리기

Apps Script → **배포 › 배포 관리 › ✏️ › 버전** 에서 **`2026-07-27-cache1`** 를 고르고 배포하세요.
암호를 묻지 않던 상태로 즉시 돌아갑니다. 웹은 건드릴 필요 없습니다.
(웹은 암호를 물어보지만, 넣지 않아도 자료는 그냥 보입니다.)

### 웹까지 되돌리기

```bash
git revert --no-edit before-auth-20260728..HEAD
```

```bash
git push origin main
```

### 나만 못 들어갈 때 (남들은 되는데)

내 브라우저에 기억된 암호가 옛것입니다.
설정 ⚙ → **「이 브라우저에서 로그아웃」** 을 누르고 다시 넣으세요.

### 관리자 암호를 잊었을 때

시트에서 **`계정` 탭**을 펼치세요 (탭 목록 오른쪽 클릭 → 숨기기 취소).
그 탭의 줄을 지우면 그 사람은 못 들어옵니다.
**모든 줄을 지우고 탭째 삭제**하면, 다음에 열 때 처음 두 계정이 다시 만들어집니다.

> `계정` 탭의 「열쇠」 칸은 암호가 아니라 섞어놓은 값입니다.
> 그 값을 그대로 넣어도 로그인되지 않습니다. 암호를 잊으면 새로 정하는 수밖에 없습니다.

---

## 4. 확인용 주소

- 시트 스크립트 버전 : 저장용 주소 뒤에 `?action=ping` 을 붙여서 열기
- 웹 버전 : 사이트에서 `Ctrl+U` → 위쪽 `?v=` 뒤의 값
- 어느 길로 자료를 받는지 : 화면 위 「데이터 기준」 옆 표시
  - `스크립트 경유` = 새 방식 (시트를 잠가도 됨)
  - `시트 직접 읽기` = 예전 방식 (시트가 열려 있어야 함)
