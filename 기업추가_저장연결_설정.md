# 기업 추가·수정 저장 연결 설정

대시보드의 `기업 추가`, 기업 정보 `수정`, 사업장 정보, 1·2차 컨설팅 일정과 동행 정보, `2주 연장`, 서류 수정, 코치 공통 서류 수정을 Google Sheet에 저장하려면 아래 설정이 필요합니다.

1. 연결된 Google Sheet를 엽니다.
2. `확장 프로그램` → `Apps Script`를 선택합니다.
3. 기본 코드를 모두 지웁니다.
4. 같은 폴더의 `GoogleSheet_기업저장.gs` 내용을 전부 복사해 붙여넣습니다.
5. 저장한 뒤 처음 설치라면 `배포` → `새 배포` → `웹 앱`을 선택합니다.
6. 실행 사용자는 `나`, 액세스 권한은 `모든 사용자`로 설정합니다.
7. 배포 후 발급된 `/exec`로 끝나는 주소를 복사합니다.
8. 대시보드 상단의 톱니바퀴를 누릅니다.
9. `기업 저장용 Apps Script 주소`에 붙여넣고 `연결 확인`을 누릅니다.
10. 성공 메시지가 나오면 `저장 후 동기화`를 누릅니다.

기존에 웹 앱을 배포한 적이 있다면 코드를 교체한 뒤 `배포` → `배포 관리` → 연필 아이콘 → `새 버전`으로 업데이트하세요. 기존 `/exec` 주소는 그대로 사용합니다.

저장되는 위치:

- `AI훈련로드맵`: 기업 기본정보와 기존 `방문(최근일자)`의 담당·일자·시간
- `작성`의 기존 앞쪽 열: `근로자수`, `사업장 관리번호`, `주소`, `공단지사`, `HRD4U`
- `작성`의 마지막 열 뒤: 기업별 종료기한 `2주 연장` 체크
- `AI훈련로드맵` 마지막 열 뒤: `1차 컨설팅일·시간·방문·담당`, `2차 컨설팅일·시간·방문·담당`
- `서류`: 기업별 신청·확정·실시·지급 서류
- `훈련코치` H~K열: 코치 공통 서식8·9·10과 통장 사본

사업장 정보 또는 1·2차 일정 열이 아직 없으면 기업을 처음 저장하거나 수정할 때 기존 열 뒤에 자동으로 추가됩니다. 이미 같은 이름의 열이 있으면 새로 만들지 않고 해당 열을 사용합니다. 동행을 체크한 일정이 두 개면 더 최근 일정이 기존 `방문(최근일자)`에도 반영됩니다.
새 기업의 기본 진행현황은 `검토요청`이며, 대시보드 파이프라인에서도 `검토요청 → 검토완료 → 컨설팅진행 → 보고서제출 → 지급준비` 순서로 표시됩니다.
같은 기업명이 이미 존재하면 중복 등록을 막습니다.
기업 목록의 `수정` 버튼에서는 진행현황, 담당자, 기업 담당자 연락처, 근로자 수·사업장 관리번호·주소·공단지사·HRD4U, 일정, 배정 코치와 서류를 변경할 수 있습니다. 사업장 관리번호 헤더가 공백 유무만 다르게 중복되어 있어도 가장 앞쪽 열을 사용하며, HRD 계정은 `HRD4U` 헤더만 사용합니다.
`2주 연장`을 처음 저장하면 작성 탭 마지막 열 뒤에 같은 이름의 열이 자동 생성됩니다. 체크하면 `O`, 해제하면 빈칸으로 저장되므로 다른 PC와 시트에서도 같은 상태를 확인할 수 있습니다.
주소를 입력하면 관할 공단지사를 자동 추천하며, 사용자가 직접 고른 값이나 기존 저장값은 자동으로 덮어쓰지 않습니다. `주소로 찾기`를 누르면 현재 주소 기준 추천을 다시 적용합니다.
서식8·9·10과 통장 사본은 코치 공통값이므로 한 곳에서 수정하면 해당 코치가 맡은 모든 기업의 서류 현황에 같이 반영됩니다.

## 작업로그 설정

새 Apps Script 버전을 배포하면 웹에서 수행한 기업 추가·수정, 신청취소·복원, 기업 서류, 코치 공통 서류 저장이 `작업로그` 탭에 자동 기록됩니다. 탭이 없으면 첫 기록 때 자동 생성됩니다. 저장에 실패하면 화면 상단에서 같은 요청을 다시 저장할 수 있습니다.

Google Sheet에서 사람이 직접 수정한 셀과 행·열 추가·삭제 같은 구조 변경도 기록하려면 Apps Script 편집기에서 다음 작업을 진행합니다. 예전에 `installAuditTrigger`를 실행한 적이 있어도 새 버전에서는 구조 변경 트리거가 추가되므로 한 번 더 실행해야 합니다.

1. 상단 함수 목록에서 `installAuditTrigger`를 선택합니다.
2. `실행`을 누르고 권한 요청을 승인합니다.
3. 실행 결과가 `auditSheetEdit + auditSheetChange 설치 완료`인지 확인합니다.
4. `작업로그` 탭이 생성됐는지 확인합니다.
5. 웹의 `작업 로그`를 열어 `시트 감사 트리거 설치됨`이라고 표시되는지 확인합니다.
6. 시트에서 아무 셀이나 한 번 수정한 뒤 웹의 작업 로그를 새로고침해 `최근 직접 수정` 시각과 `셀 직접 수정` 기록이 나타나는지 확인합니다.

직접 수정 로그는 스프레드시트의 모든 탭을 기록합니다. `작업로그` 자체 수정은 재귀 기록을 막기 위해 제외됩니다. 웹 작업은 변경 전후 값까지 기록하고, 시트의 단일 셀 수정은 Google이 제공하는 이전값과 수정값을 기록합니다. 여러 셀을 한꺼번에 붙여넣은 작업과 행·열 구조 변경은 작업 범위 또는 변경 종류가 기록되며, 수식의 자동 재계산 자체는 사용자 편집이 아니므로 별도 로그를 만들지 않습니다.
