# 작업 규칙

이 프로젝트에서 반복해서 걸렸던 함정을 적어둔다. 작업 전에 훑고, 새로 걸린 게 생기면 여기에 추가한다.

---

## 1. 코드를 고치면 버전 번호를 반드시 같이 올린다

**두 번 걸렸다.** 고쳤는데 화면이 그대로여서 한참 헤맸고, 한 번은 **고쳐진 줄 착각**한 채로 「검증 통과」라고 잘못 보고했다.

버전이 적힌 곳은 **세 군데**이고, 짝을 맞춰야 한다.

| 무엇을 고쳤나 | 같이 올릴 곳 |
|---|---|
| `js/*.js`, `assets/styles.css` | `js/config.js`의 `APP_ASSET_VERSION` **그리고** `index.html`의 `?v=` **12곳** |
| `GoogleSheet_기업저장.gs` | 파일 맨 위 `VERSION` |

```bash
# 화면 쪽 — 두 곳을 한 번에
sed -i "s/ux4/ux5/" js/config.js
sed -i 's/?v=20260726-ux4/?v=20260726-ux5/g' index.html
```

**왜 위험한가** — 번호가 같으면 `ping`으로 확인해도 「최신」으로 보인다. 실제로는 옛 코드가 도는데 새 코드가 도는 줄 알게 된다. 브라우저도 마찬가지로 옛 파일을 계속 쓴다.

**확인 방법**
- 화면 쪽 → 브라우저에서 `APP_ASSET_VERSION` 값
- 시트 쪽 → `/exec?action=ping` 의 `version` 값

`.gs`는 고친 뒤 **배포 › 배포 관리 › ✏️ › 버전: 새 버전 › 배포** 를 해야 반영된다. 「새 배포」를 누르면 주소가 새로 생기니 쓰지 않는다.

---

## 2. 탭 이름은 `getSheetByName`으로 확인한다. gviz를 믿지 않는다

**하루를 날린 함정이다.**

gviz(`/gviz/tq?sheet=이름`)는 **없는 탭 이름을 넣어도 오류를 내지 않고 첫 번째 탭을 대신 돌려준다.** 그래서 `AI훈련로드맵` 탭이 없는데도 있는 것처럼 보였고, 두 이름이 똑같은 행 수를 보이는 걸 보고서야 알아챘다.

- 탭이 실제로 있는지 확인할 때는 `?action=diag` 를 쓴다. 모든 탭 이름을 그대로 돌려준다.
- 서로 다른 이름을 물었는데 **행 수가 똑같으면** 같은 탭을 보고 있는 것이다.

기업 원본 탭은 이름이 `작성` ↔ `AI훈련로드맵` 으로 오간 적이 있어, `.gs`의 `SOURCE_SHEET_NAMES` 가 둘 다 받아들인다.

---

## 3. 시트 구조를 바꾸는 작업은 먼저 «읽어보고» 한다

수식으로 채워지는 탭에 행을 추가했다가 `#REF!` 로 전체가 날아간 적이 있다. 그 상태에서 「값만 붙여넣기」를 하면 **오류가 그대로 굳어 되돌릴 수 없다.**

- 쓰기 전에 `?action=diag` 로 탭 구성과 크기를 확인한다
- `서류` 탭의 B~E, M~O, T 열은 **수식이 채운다.** 값을 직접 쓰지 않는다
- 수식이 펼쳐질 자리에 **병합된 셀**이 있으면 수식이 들어가지 않는다. 안 먹으면 병합부터 푼다

---

## 4. 운영 데이터로 시험할 때는 되돌릴 방법을 먼저 정한다

- 값을 바꾸는 시험은 **원래 값을 먼저 저장**해두고, 끝나면 되돌린다
- 기업 추가는 **행을 지울 방법이 없다.** 시험용으로 넣으면 시트에서 직접 지워야 하므로, 넣기 전에 사용자에게 알린다
- 통신만 확인하면 되는 경우 `requestSheetWrite` 를 임시로 바꿔치기해 **시트를 건드리지 않고** 시험한다

---

## 5. 헤더 이름이 한 글자만 달라도 열을 못 찾는다

`사업장관리번호` 와 `사업장 관리번호`(공백) 처럼 미세하게 다른 헤더가 섞여 있어, 같은 항목이 두 열로 갈라진 적이 있다. 값은 저장됐는데 화면에는 안 보이는 형태로 나타난다.

- 저장은 되는데 화면에 안 보이면 **헤더 이름부터** 확인한다
- 새 열을 붙일 때는 기존 헤더를 먼저 훑고, 공백 차이까지 본다

---

## 6. 설명은 코드 용어 없이 한다

사용자는 코드를 읽지 않는다. **«이렇게 하면 이게 된다»** 로 쓴다.

- ❌ `editDocCell`에서 keydown 핸들러가 `commit(0,1)`을 호출해 포커스를 옮긴다
- ✅ 셀에서 Enter를 치면 아래 칸으로 넘어가면서 바로 입력할 수 있다

검증 결과는 `✅ 됨 / ❌ 안 됨` 으로 보여준다. 파일명·줄 번호는 참고로만 뒤에 붙인다.
