"use strict";
/*
 * 비상용 내장 스냅샷 — «비워둔다».
 *
 * 예전에는 여기에 시트 내용을 통째로 넣어두고, 연결이 안 될 때 대신 보여줬다.
 * 그런데 이 파일은 웹에 그대로 올라가므로 담당자 연락처까지 같이 공개된다.
 * 그래서 실제 값은 넣지 않고, 자료는 항상 Google Sheet에서 받아 온다.
 *
 * 한 번이라도 동기화한 브라우저는 그 내용을 기억하고 있어 다음에 열 때 바로 보인다.
 * 처음 여는 브라우저는 시트에서 받아오는 동안 «불러오는 중» 화면이 잠깐 보인다.
 */
const EMBEDDED = { generatedAt: '', sheets: {} };
