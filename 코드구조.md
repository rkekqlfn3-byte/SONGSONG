# AI훈련로드맵 운영 콘솔 코드 구조

`index.html`은 화면 뼈대이자 GitHub Pages의 첫 화면이며, 아래 파일을 순서대로 불러옵니다. 별도 설치나 빌드 없이 HTML 파일을 직접 열어 사용할 수 있습니다.

## 파일별 역할

- `assets/styles.css` — 전체 디자인, 반응형·모바일 스타일
- `js/data.js` — 비상용 내장 Google Sheet 스냅샷
- `js/core.js` — 공용 함수, 데이터 정규화, 앱 상태와 화면 전환
- `js/views.js` — 대시보드, 기업 목록, 코치 목록
- `js/documents.js` — 서류 현황 매트릭스, 셀 편집, 기업·코치 상세 패널
- `js/mail.js` — 메일 작성기
- `js/sheet-api.js` — Google Sheet 읽기·쓰기, 동기화, 파일 불러오기
- `js/company-form.js` — 기업 추가·수정 입력창과 저장 검증
- `js/app.js` — 이벤트 연결과 앱 시작
- `GoogleSheet_기업저장.gs` — Google Sheet에 설치하는 Apps Script

## 유지보수 규칙

1. HTML 아래쪽의 JavaScript 로드 순서는 바꾸지 않습니다.
2. 화면 디자인은 `assets/styles.css`에서 수정합니다.
3. 서류 종류와 상태 정의는 `js/core.js`, 서류 입력 동작은 `js/documents.js`에서 수정합니다.
4. Google Sheet 통신 형식을 바꿀 때는 `js/sheet-api.js`와 `GoogleSheet_기업저장.gs`를 함께 확인합니다.
5. 기능 수정 후 모든 JavaScript 파일의 문법과 HTML에 선언된 파일 경로를 검증합니다.
